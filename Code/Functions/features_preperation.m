function [X_train, Y_train, X_test, Y_test] = features_preperation(X_train, Y_train, X_test, Y_test)
    % NO NEED, ALREADY prepared with proper baseline and normalization in
    % the function feature_extraction.
    X_train = X_train;
    Y_train = Y_train;
    X_test  = X_test;
    Y_test  = Y_test;
end